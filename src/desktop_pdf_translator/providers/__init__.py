"""Translation/chat providers: what each one is, and how to reach its models.

Re-exports nothing. `providers.registry` is imported on the sidecar's boot path
(by `config.models`), and every name added here would run on that path too.
"""
